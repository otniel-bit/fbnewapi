{
  "name": "fbnewapi-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Commas API & SDK documentation — single-file static site (index.html), served by Netlify. The changelog section is generated from changelog.json on deploy; the SDK scripts keep the SDK reference in sync with @fanbasis/checkout-core.",
  "scripts": {
    "changelog": "node scripts/build-changelog.mjs",
    "changelog:check": "node scripts/build-changelog.mjs --check",
    "check:sdk": "node scripts/check-sdk.mjs",
    "gen:sdk": "node scripts/check-sdk.mjs --write",
    "check:sdk:npm": "node scripts/check-sdk.mjs --check-npm"
  },
  "engines": {
    "node": ">=20"
  }
}
